<!-- Enhanced Professional Navigation with Design System -->
<nav class="bg-indigo-600 shadow-lg" style="position: relative; z-index: 10000;">
  <div class="w-full px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      
      <!-- Left: Logo & Brand -->
      <div class="flex items-center flex-shrink-0">
        <%= link_to root_path, class: "flex items-center space-x-3 group" do %>
          <%= image_tag 'moopio-logo.png', class: 'h-10 w-10 transition-transform duration-200 group-hover:scale-110', alt: 'Morfeo Analytics' %>
          <div class="hidden lg:block">
            <span class="text-white text-xl font-bold tracking-tight">Morfeo</span>
            <span class="block text-indigo-200 text-xs font-medium">Media Analytics</span>
          </div>
        <% end %>
      </div>

      <!-- Center: Main Navigation (Desktop) -->
      <div class="hidden lg:flex items-center space-x-1">
        
        <!-- Home Link -->
        <%= link_to "Inicio", root_path, 
            class: [
              "px-4 py-2 rounded-lg text-sm font-medium transition-all duration-200",
              (request.path == root_path ? "bg-indigo-700 text-white" : "text-indigo-100 hover:bg-indigo-700 hover:text-white")
            ] %>

        <% topics_for_dropdown = (@topicos.present? ? @topicos : Topic.active).to_a %>

        <!-- Digitales Dropdown -->
        <div class="relative" x-data="{ open: false }" @click.away="open = false">
          <button @click="open = !open"
                  class="inline-flex items-center px-4 py-2 rounded-lg text-sm font-medium text-indigo-100 hover:bg-indigo-700 hover:text-white transition-all duration-200"
                  :class="{ 'bg-indigo-700 text-white': open }"
                  aria-expanded="false"
                  aria-haspopup="true">
            <i class="fa-solid fa-newspaper w-4 h-4 mr-2"></i>
            Digitales
            <svg class="w-4 h-4 ml-2 transition-transform duration-200" :class="{ 'rotate-180': open }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>

          <div x-show="open"
               x-cloak
               x-transition:enter="transition ease-out duration-200"
               x-transition:enter-start="opacity-0 scale-95"
               x-transition:enter-end="opacity-100 scale-100"
               x-transition:leave="transition ease-in duration-150"
               x-transition:leave-start="opacity-100 scale-100"
               x-transition:leave-end="opacity-0 scale-95"
               class="absolute left-0 mt-2 w-64 origin-top-left rounded-xl bg-white shadow-2xl ring-1 ring-black ring-opacity-5 focus:outline-none"
               style="z-index: 10001;"
               role="menu">
            <div class="py-2">
              <% topics_for_dropdown.each do |topic| %>
                <%= link_to topic.name, topic_path(topic),
                            class: [
                              "block px-4 py-2 text-sm transition-colors duration-150",
                              (request.path == topic_path(topic) ? "bg-indigo-50 text-indigo-700 font-medium" : "text-gray-700 hover:bg-gray-50")
                            ] %>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Facebook Dropdown -->
        <div class="relative" x-data="{ open: false }" @click.away="open = false">
          <button @click="open = !open"
                  class="inline-flex items-center px-4 py-2 rounded-lg text-sm font-medium text-indigo-100 hover:bg-indigo-700 hover:text-white transition-all duration-200"
                  :class="{ 'bg-indigo-700 text-white': open }"
                  aria-expanded="false"
                  aria-haspopup="true">
            <i class="fa-brands fa-facebook w-4 h-4 mr-2"></i>
            Facebook
            <svg class="w-4 h-4 ml-2 transition-transform duration-200" :class="{ 'rotate-180': open }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>

          <div x-show="open"
               x-cloak
               x-transition:enter="transition ease-out duration-200"
               x-transition:enter-start="opacity-0 scale-95"
               x-transition:enter-end="opacity-100 scale-100"
               x-transition:leave="transition ease-in duration-150"
               x-transition:leave-start="opacity-100 scale-100"
               x-transition:leave-end="opacity-0 scale-95"
               class="absolute left-0 mt-2 w-64 origin-top-left rounded-xl bg-white shadow-2xl ring-1 ring-black ring-opacity-5 focus:outline-none"
               style="z-index: 10001;"
               role="menu">
            <div class="py-2">
              <% topics_for_dropdown.each do |topic| %>
                <%= link_to topic.name, facebook_topic_path(topic),
                            class: [
                              "block px-4 py-2 text-sm transition-colors duration-150",
                              (request.path == facebook_topic_path(topic) ? "bg-blue-50 text-blue-700 font-medium" : "text-gray-700 hover:bg-gray-50")
                            ] %>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Twitter Dropdown -->
        <div class="relative" x-data="{ open: false }" @click.away="open = false">
          <button @click="open = !open"
                  class="inline-flex items-center px-4 py-2 rounded-lg text-sm font-medium text-indigo-100 hover:bg-indigo-700 hover:text-white transition-all duration-200"
                  :class="{ 'bg-indigo-700 text-white': open }"
                  aria-expanded="false"
                  aria-haspopup="true">
            <i class="fa-brands fa-twitter w-4 h-4 mr-2"></i>
            Twitter
            <svg class="w-4 h-4 ml-2 transition-transform duration-200" :class="{ 'rotate-180': open }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>

          <div x-show="open"
               x-cloak
               x-transition:enter="transition ease-out duration-200"
               x-transition:enter-start="opacity-0 scale-95"
               x-transition:enter-end="opacity-100 scale-100"
               x-transition:leave="transition ease-in duration-150"
               x-transition:leave-start="opacity-100 scale-100"
               x-transition:leave-end="opacity-0 scale-95"
               class="absolute left-0 mt-2 w-64 origin-top-left rounded-xl bg-white shadow-2xl ring-1 ring-black ring-opacity-5 focus:outline-none"
               style="z-index: 10001;"
               role="menu">
            <div class="py-2">
              <% topics_for_dropdown.each do |topic| %>
                <%= link_to topic.name, twitter_topic_path(topic),
                            class: [
                              "block px-4 py-2 text-sm transition-colors duration-150",
                              (request.path == twitter_topic_path(topic) ? "bg-sky-50 text-sky-700 font-medium" : "text-gray-700 hover:bg-gray-50")
                            ] %>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Instagram Dropdown -->
        <div class="relative" x-data="{ open: false }" @click.away="open = false">
          <button @click="open = !open"
                  class="inline-flex items-center px-4 py-2 rounded-lg text-sm font-medium text-indigo-100 hover:bg-indigo-700 hover:text-white transition-all duration-200"
                  :class="{ 'bg-indigo-700 text-white': open }"
                  aria-expanded="false"
                  aria-haspopup="true">
            <i class="fa-brands fa-instagram w-4 h-4 mr-2"></i>
            Instagram
            <svg class="w-4 h-4 ml-2 transition-transform duration-200" :class="{ 'rotate-180': open }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>

          <div x-show="open"
               x-cloak
               x-transition:enter="transition ease-out duration-200"
               x-transition:enter-start="opacity-0 scale-95"
               x-transition:enter-end="opacity-100 scale-100"
               x-transition:leave="transition ease-in duration-150"
               x-transition:leave-start="opacity-100 scale-100"
               x-transition:leave-end="opacity-0 scale-95"
               class="absolute left-0 mt-2 w-64 origin-top-left rounded-xl bg-white shadow-2xl ring-1 ring-black ring-opacity-5 focus:outline-none"
               style="z-index: 10001;"
               role="menu">
            <div class="py-2">
              <% topics_for_dropdown.each do |topic| %>
                <%= link_to topic.name, instagram_topic_path(topic),
                            class: [
                              "block px-4 py-2 text-sm transition-colors duration-150",
                              (request.path == instagram_topic_path(topic) ? "bg-pink-50 text-pink-700 font-medium" : "text-gray-700 hover:bg-gray-50")
                            ] %>
              <% end %>
            </div>
          </div>
        </div>

        <!-- General Dashboard Dropdown -->
        <div class="relative" x-data="{ open: false }" @click.away="open = false">
          <button @click="open = !open"
                  class="inline-flex items-center px-4 py-2 rounded-lg text-sm font-medium text-indigo-100 hover:bg-indigo-700 hover:text-white transition-all duration-200"
                  :class="{ 'bg-indigo-700 text-white': open }"
                  aria-expanded="false"
                  aria-haspopup="true">
            <i class="fa-solid fa-chart-pie w-4 h-4 mr-2"></i>
            General
            <svg class="w-4 h-4 ml-2 transition-transform duration-200" :class="{ 'rotate-180': open }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>

          <div x-show="open"
               x-cloak
               x-transition:enter="transition ease-out duration-200"
               x-transition:enter-start="opacity-0 scale-95"
               x-transition:enter-end="opacity-100 scale-100"
               x-transition:leave="transition ease-in duration-150"
               x-transition:leave-start="opacity-100 scale-100"
               x-transition:leave-end="opacity-0 scale-95"
               class="absolute left-0 mt-2 w-64 origin-top-left rounded-xl bg-white shadow-2xl ring-1 ring-black ring-opacity-5 focus:outline-none"
               style="z-index: 10001;"
               role="menu">
            <div class="py-2">
              <% topics_for_dropdown.each do |topic| %>
                <%= link_to topic.name, general_dashboard_path(topic),
                            class: [
                              "block px-4 py-2 text-sm transition-colors duration-150",
                              (request.path == general_dashboard_path(topic) ? "bg-purple-50 text-purple-700 font-medium" : "text-gray-700 hover:bg-gray-50")
                            ] %>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Reports Dropdown -->
        <div class="relative" x-data="{ open: false }" @click.away="open = false">
          <button @click="open = !open"
                  class="inline-flex items-center px-4 py-2 rounded-lg text-sm font-medium text-indigo-100 hover:bg-indigo-700 hover:text-white transition-all duration-200"
                  :class="{ 'bg-indigo-700 text-white': open }">
            <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M3 3a1 1 0 000 2v8a2 2 0 002 2h2.586l-1.293 1.293a1 1 0 101.414 1.414L10 15.414l2.293 2.293a1 1 0 001.414-1.414L12.414 15H15a2 2 0 002-2V5a1 1 0 100-2H3zm11.707 4.707a1 1 0 00-1.414-1.414L10 9.586 8.707 8.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"/>
            </svg>
            Reportes
            <svg class="w-4 h-4 ml-2 transition-transform duration-200" :class="{ 'rotate-180': open }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>

          <div x-show="open"
               x-cloak
               x-transition:enter="transition ease-out duration-200"
               x-transition:enter-start="opacity-0 scale-95"
               x-transition:enter-end="opacity-100 scale-100"
               x-transition:leave="transition ease-in duration-150"
               x-transition:leave-start="opacity-100 scale-100"
               x-transition:leave-end="opacity-0 scale-95"
               class="absolute right-0 mt-2 w-56 origin-top-right rounded-xl bg-white shadow-xl ring-1 ring-black ring-opacity-5 focus:outline-none"
               style="z-index: 10001;">
            <div class="py-2">
              <%= link_to popular_entries_path, class: "flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 transition-colors" do %>
                <i class="fa-solid fa-fire text-orange-500 w-5 mr-3"></i>
                <span>Más Populares</span>
              <% end %>
              <%= link_to commented_entries_path, class: "flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 transition-colors" do %>
                <i class="fa-solid fa-comments text-green-600 w-5 mr-3"></i>
                <span>Más Comentadas</span>
              <% end %>
              <%= link_to week_entries_path, class: "flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 transition-colors" do %>
                <i class="fa-solid fa-calendar-week text-indigo-600 w-5 mr-3"></i>
                <span>Resumen Semanal</span>
              <% end %>
            </div>
          </div>
        </div>

      </div>

      <!-- Right: User Menu & Actions -->
      <div class="flex items-center space-x-4">
        
        <!-- Search (Desktop) - Future Enhancement -->
        <!-- <button class="hidden lg:inline-flex p-2 rounded-lg text-indigo-100 hover:bg-indigo-700 hover:text-white transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"/>
          </svg>
        </button> -->

        <!-- Notifications - Future Enhancement -->
        <!-- <button class="hidden lg:inline-flex relative p-2 rounded-lg text-indigo-100 hover:bg-indigo-700 hover:text-white transition-colors">
          <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"/>
          </svg>
          <span class="absolute top-1 right-1 w-2 h-2 bg-red-500 rounded-full"></span>
        </button> -->

        <!-- User Profile Dropdown -->
        <div class="relative" x-data="{ open: false }" @click.away="open = false">
          <button @click="open = !open"
                  class="flex items-center space-x-2 p-1 rounded-lg hover:bg-indigo-700 transition-colors"
                  aria-expanded="false"
                  aria-haspopup="true">
            <div class="hidden lg:block text-right">
              <p class="text-sm font-medium text-white"><%= current_user.email.split('@').first.capitalize %></p>
              <p class="text-xs text-indigo-200">Usuario</p>
            </div>
            <div class="w-10 h-10 bg-indigo-700 rounded-full flex items-center justify-center text-white font-semibold ring-2 ring-indigo-400 hover:ring-white transition-all">
              <%= current_user.email[0].upcase %>
            </div>
            <svg class="hidden lg:block w-4 h-4 text-indigo-200 transition-transform duration-200" :class="{ 'rotate-180': open }" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd"/>
            </svg>
          </button>

          <div x-show="open"
               x-cloak
               x-transition:enter="transition ease-out duration-200"
               x-transition:enter-start="opacity-0 scale-95"
               x-transition:enter-end="opacity-100 scale-100"
               x-transition:leave="transition ease-in duration-150"
               x-transition:leave-start="opacity-100 scale-100"
               x-transition:leave-end="opacity-0 scale-95"
               class="absolute right-0 mt-2 w-56 origin-top-right rounded-xl bg-white shadow-xl ring-1 ring-black ring-opacity-5 focus:outline-none"
               style="z-index: 10001;">
            
            <!-- User Info -->
            <div class="px-4 py-3 border-b border-gray-100">
              <p class="text-sm font-medium text-gray-900"><%= current_user.email.split('@').first.capitalize %></p>
              <p class="text-xs text-gray-500 truncate"><%= current_user.email %></p>
            </div>

            <!-- Menu Items -->
            <div class="py-2">
              <!-- Future enhancement: User profile and settings -->
              <!-- <a href="#" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 transition-colors">
                <i class="fa-solid fa-user w-5 mr-3 text-gray-400"></i>
                <span>Mi Perfil</span>
              </a>
              <a href="#" class="flex items-center px-4 py-2 text-sm text-gray-700 hover:bg-gray-50 transition-colors">
                <i class="fa-solid fa-cog w-5 mr-3 text-gray-400"></i>
                <span>Configuración</span>
              </a> -->
              
              <!-- Note: Admin panel uses separate AdminUser authentication -->
              <!-- Regular users don't have access to admin panel -->
            </div>

            <!-- Logout -->
            <div class="border-t border-gray-100">
              <%= link_to destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "flex items-center px-4 py-3 text-sm text-red-700 hover:bg-red-50 transition-colors rounded-b-xl" do %>
                <i class="fa-solid fa-arrow-right-from-bracket w-5 mr-3"></i>
                <span>Cerrar Sesión</span>
              <% end %>
            </div>
          </div>
        </div>

        <!-- Mobile Menu Toggle -->
        <button @click="mobileMenuOpen = !mobileMenuOpen"
                x-data="{ mobileMenuOpen: false }"
                class="lg:hidden p-2 rounded-lg text-white hover:bg-indigo-700 transition-colors">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
          </svg>
        </button>

      </div>
    </div>

    <!-- Mobile Menu -->
    <div x-show="mobileMenuOpen"
         x-cloak
         x-data="{ mobileMenuOpen: false }"
         x-transition:enter="transition ease-out duration-200"
         x-transition:enter-start="opacity-0 -translate-y-2"
         x-transition:enter-end="opacity-100 translate-y-0"
         x-transition:leave="transition ease-in duration-150"
         x-transition:leave-start="opacity-100 translate-y-0"
         x-transition:leave-end="opacity-0 -translate-y-2"
         class="lg:hidden border-t border-indigo-700 py-3 w-full"
         id="mobile-menu">
      
      <div class="space-y-1">
        <%= link_to "Inicio", root_path, 
            class: [
              "block px-4 py-2 text-sm font-medium rounded-lg transition-colors",
              (request.path == root_path ? "bg-indigo-700 text-white" : "text-indigo-100 hover:bg-indigo-700 hover:text-white")
            ] %>

        <% topics_for_mobile = (@topicos.present? ? @topicos : Topic.active).to_a %>

        <!-- Mobile Digitales Topics -->
        <div class="px-4 py-2">
          <p class="text-xs font-semibold text-indigo-200 uppercase tracking-wider mb-2 flex items-center">
            <i class="fa-solid fa-newspaper mr-2"></i>Digitales
          </p>
          <% topics_for_mobile.each do |topic| %>
            <%= link_to topic.name, topic_path(topic), class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
          <% end %>
        </div>

        <!-- Mobile Facebook Topics -->
        <div class="px-4 py-2">
          <p class="text-xs font-semibold text-indigo-200 uppercase tracking-wider mb-2 flex items-center">
            <i class="fa-brands fa-facebook mr-2"></i>Facebook
          </p>
          <% topics_for_mobile.each do |topic| %>
            <%= link_to topic.name, facebook_topic_path(topic), class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
          <% end %>
        </div>

        <!-- Mobile Twitter Topics -->
        <div class="px-4 py-2">
          <p class="text-xs font-semibold text-indigo-200 uppercase tracking-wider mb-2 flex items-center">
            <i class="fa-brands fa-twitter mr-2"></i>Twitter
          </p>
          <% topics_for_mobile.each do |topic| %>
            <%= link_to topic.name, twitter_topic_path(topic), class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
          <% end %>
        </div>

        <!-- Mobile Instagram Topics -->
        <div class="px-4 py-2">
          <p class="text-xs font-semibold text-indigo-200 uppercase tracking-wider mb-2 flex items-center">
            <i class="fa-brands fa-instagram mr-2"></i>Instagram
          </p>
          <% topics_for_mobile.each do |topic| %>
            <%= link_to topic.name, instagram_topic_path(topic), class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
          <% end %>
        </div>

        <!-- Mobile General Dashboard -->
        <div class="px-4 py-2">
          <p class="text-xs font-semibold text-indigo-200 uppercase tracking-wider mb-2 flex items-center">
            <i class="fa-solid fa-chart-pie mr-2"></i>General
          </p>
          <% topics_for_mobile.each do |topic| %>
            <%= link_to topic.name, general_dashboard_path(topic), class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
          <% end %>
        </div>

        <!-- Mobile Reports -->
        <div class="px-4 py-2 border-t border-indigo-700 mt-2 pt-2">
          <p class="text-xs font-semibold text-indigo-200 uppercase tracking-wider mb-2">Reportes</p>
          <%= link_to "Más Populares", popular_entries_path, class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
          <%= link_to "Más Comentadas", commented_entries_path, class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
          <%= link_to "Resumen Semanal", week_entries_path, class: "block py-2 pl-4 text-sm text-indigo-100 hover:text-white transition-colors" %>
        </div>
      </div>
    </div>

  </div>
</nav>

<script>
  // Close dropdowns on escape key
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape') {
      // Trigger click away event
      document.dispatchEvent(new Event('click'));
    }
  });
</script>
