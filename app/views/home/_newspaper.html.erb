<% if newspaper.cover.present? %>
  <div class="group relative bg-white rounded-xl shadow-md hover:shadow-xl transition-all duration-300 overflow-hidden border border-gray-200 hover:border-indigo-300">
    <!-- Image Container -->
    <div class="relative aspect-[3/4] bg-gray-50 overflow-hidden">
      <%= link_to url_for(newspaper.cover), target: '_blank', class: 'block w-full h-full' do %>
        <%= image_tag(newspaper.cover, 
                      loading: 'lazy', 
                      title: "Click para ver en tamaño completo", 
                      class: 'w-full h-full object-cover object-center group-hover:scale-105 transition-transform duration-300') %>
        <!-- Hover Overlay -->
        <div class="absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-10 transition-all duration-300 flex items-center justify-center">
          <div class="opacity-0 group-hover:opacity-100 transition-opacity duration-300">
            <div class="bg-white rounded-full p-3 shadow-lg">
              <i class="fa-solid fa-magnifying-glass-plus text-indigo-600 text-xl"></i>
            </div>
          </div>
        </div>
      <% end %>
      
      <!-- Badge -->
      <div class="absolute top-3 left-3">
        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-indigo-600 text-white shadow-lg">
          <i class="fa-solid fa-newspaper mr-1"></i>
          Portada
        </span>
      </div>
    </div>
    
    <!-- Info Footer -->
    <div class="p-4 bg-gradient-to-br from-gray-50 to-white">
      <div class="flex items-center justify-between">
        <div class="flex-1 min-w-0">
          <h3 class="text-sm font-semibold text-gray-900 truncate mb-1">
            <%= newspaper.site.name %>
          </h3>
          <p class="text-xs text-gray-500 flex items-center">
            <i class="fa-solid fa-calendar-days mr-1"></i>
            <%= newspaper.date.strftime("%d/%m/%Y") %>
          </p>
        </div>
        <%= link_to newspaper.site, target: '_blank', class: 'ml-2 inline-flex items-center justify-center w-8 h-8 rounded-lg bg-indigo-50 text-indigo-600 hover:bg-indigo-100 transition-colors' do %>
          <i class="fa-solid fa-arrow-up-right-from-square text-sm"></i>
        <% end %>
      </div>
    </div>
  </div>
<% end %>

<% if newspaper.backcover.present? %>
  <div class="group relative bg-white rounded-xl shadow-md hover:shadow-xl transition-all duration-300 overflow-hidden border border-gray-200 hover:border-purple-300">
    <!-- Image Container -->
    <div class="relative aspect-[3/4] bg-gray-50 overflow-hidden">
      <%= link_to url_for(newspaper.backcover), target: '_blank', class: 'block w-full h-full' do %>
        <%= image_tag(newspaper.backcover, 
                      loading: 'lazy', 
                      title: "Click para ver en tamaño completo", 
                      class: 'w-full h-full object-cover object-center group-hover:scale-105 transition-transform duration-300') %>
        <!-- Hover Overlay -->
        <div class="absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-10 transition-all duration-300 flex items-center justify-center">
          <div class="opacity-0 group-hover:opacity-100 transition-opacity duration-300">
            <div class="bg-white rounded-full p-3 shadow-lg">
              <i class="fa-solid fa-magnifying-glass-plus text-purple-600 text-xl"></i>
            </div>
          </div>
        </div>
      <% end %>
      
      <!-- Badge -->
      <div class="absolute top-3 left-3">
        <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-medium bg-purple-600 text-white shadow-lg">
          <i class="fa-solid fa-newspaper mr-1"></i>
          Contratapa
        </span>
      </div>
    </div>
    
    <!-- Info Footer -->
    <div class="p-4 bg-gradient-to-br from-gray-50 to-white">
      <div class="flex items-center justify-between">
        <div class="flex-1 min-w-0">
          <h3 class="text-sm font-semibold text-gray-900 truncate mb-1">
            <%= newspaper.site.name %>
          </h3>
          <p class="text-xs text-gray-500 flex items-center">
            <i class="fa-solid fa-calendar-days mr-1"></i>
            <%= newspaper.date.strftime("%d/%m/%Y") %>
          </p>
        </div>
        <%= link_to newspaper.site, target: '_blank', class: 'ml-2 inline-flex items-center justify-center w-8 h-8 rounded-lg bg-purple-50 text-purple-600 hover:bg-purple-100 transition-colors' do %>
          <i class="fa-solid fa-arrow-up-right-from-square text-sm"></i>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
